<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
  
  <mapper namespace="qnaMapper">
      	<select id="boardCount" resultType="java.lang.Integer" parameterType="com.nuri.s4.util.Pager">
  			select count(num) from qna
			<choose>
				<when test="kind == 'kt'">
					where title like '%'||#{search}||'%'
				</when>
				<when test="kind == 'kw'">
					where writer like '%'||#{search}||'%'
				</when>
				<otherwise>
					where contents like	'%'||#{search}||'%'
				</otherwise>
			</choose>
  		</select>
    
    
    
    	<select id="boardList" parameterType="com.nuri.s4.util.Pager" resultType="com.nuri.s4.model.BoardNoticeVO">
  		select * from
			(select N.*, rownum R from
				(select * from qna 
				<choose>
					<when test="kind == 'kt'">
						where title like '%'||#{search}||'%'
					</when>
					<when test="kind == 'kw'">
						where writer like '%'||#{search}||'%'
					</when>
					<when test="kind == 'kc'">
						where contents like '%'||#{search}||'%'
					</when>
				</choose>
				order by num desc) N)
		where R between #{startRow} and #{lastRow}
 	 	</select>
    
    </mapper>